namespace Subjects {
  // Définition d’une interface Teacher dans le namespace Subjects
  export interface Teacher {
    // Prénom du professeur (obligatoire)
    firstName: string;

    // Nom de famille du professeur (obligatoire)
    lastName: string;
  }
}
